# 🛠️ 트러블슈팅 및 장애 재발 방지 기록 (Troubleshooting Log)

---

## 📌 이슈 [2026-09-02]: 모바일 환경에서 필터 탭(모집유형, 카테고리, 플랫폼, 지역검색) 터치 시 즉시 닫힘 장애

### 1. 현상 (Symptom)
* 모바일 기기(아이폰, 안드로이드 스마트폰, 터치 태블릿)에서 메인 페이지 상단 필터바의 `모집유형`, `카테고리`, `플랫폼`, `지역검색` 탭 버튼을 누르자마자 바텀시트/드롭다운 패널이 0.1초 만에 바로 닫혀버리는 현상 발생.

---

### 2. 원인 분석 (Root Cause Analysis)

1. **모바일 터치 이벤트 시 `onMouseEnter` ➔ `onClick` 중복 간섭 버그**
   * 모바일 웹 브라우저는 탭(터치) 시 `onMouseEnter` ➔ `onClick` 순서로 이벤트를 발생시킵니다.
   * `onMouseEnter` 이벤트가 먼저 실행되어 `isOpen = true`로 상태가 변경된 직후, 바로 `onClick` 이벤트의 `handleTabClick` 핸들러가 실행되어 `!isOpen` (`!true` = `false`)을 적용함에 따라 탭이 열리자마자 바로 닫히게 되었습니다.

2. **모바일 터치 해제 시 `onMouseLeave` 350ms 타이머 자동 실행**
   * `.filter-container-wrap` 영역에 걸려있던 `onMouseLeave` 핸들러(`handleFilterAreaLeave`)가 터치 포커스가 이동/해제될 때 트리거되었습니다.
   * 이로 인해 350ms 후 `setIsTypeOpen(false)`, `setIsCategoryOpen(false)` 등이 무조건 호출되어 모바일 화면에서 열린 패널이 350ms 만에 자동으로 닫혀버렸습니다.

---

### 3. 영구 수정 및 해결 조치 (Permanent Solution)

1. **미디어 쿼리 기반 `window.matchMedia('(hover: hover)').matches` 환경 분리 (`src/app/page.tsx`)**
   * 데스크톱(실제 마우스 포인터 디바이스)에서만 hover 이벤트(`onMouseEnter`, `onMouseLeave`, 350ms 타이머)가 동작하도록 가드 조건을 추가했습니다.
   * 터치 전용 모바일 기기에서는 `onMouseEnter` 및 `onMouseLeave`에 의한 상태 변경이 일절 실행되지 않도록 차단하여, 탭 클릭(`onClick`) 시 정상적으로 탭이 열려 유지되도록 보장했습니다.

2. **결과 검증 (Post-Fix Results)**
   * **모바일 환경에서 `모집유형`, `카테고리`, `플랫폼`, `지역검색` 터치 시**: 패널(바텀시트)이 안정적으로 열린 상태로 유지됨 🟢
   * **모바일 패널 닫기 동작**: 딤드 배경(`mobile-backdrop`) 터치, 우측 상단 `✕` 버튼, 또는 내부 필터 옵션 칩 선택 시에만 정상적으로 닫힘 🟢
   * **데스크톱 환경**: 마우스 호버 시 자연스러운 드롭다운 열림/닫힘 기능 유지 🟢

---

### 4. 재발 방지 가이드라인 (Preventive Directive)
* 모바일 터치 브라우저 특성상 hover 이벤트(`onMouseEnter`, `onMouseLeave`)가 click 이벤트와 연쇄적으로 반응하므로, UI 상태 변경 이벤트에는 반드시 `window.matchMedia('(hover: hover)').matches` 가드를 적용하여 터치 기기에서 패널이 무단으로 닫히는 일이 없도록 합니다.

---

## 📌 이슈 [2026-09-01]: 키워드 마스터 연관검색어 수치와 상세 클릭 시 검색량 불일치 장애

### 1. 현상 (Symptom)
* 키워드 마스터에서 `삼겹살` 검색 시 연관검색어 리스트의 16위(또는 카테고리 프리셋 연관어)에 `맛찬들`이 표출되었으나, 월간 총 검색량이 **10회**로 표시됨.
* 반면, 해당 `맛찬들` 키워드를 클릭하여 상세 분석 페이지로 이동하면 월간 총 검색량이 **36,130회**로 표출되는 수치 불일치 및 왜곡 현상 발생.

---

### 2. 원인 분석 (Root Cause Analysis)

1. **카테고리 프리셋 후보군의 2차 검색광고 API 조회 하드코딩 캡 (`slice(0, 5)`)**
   * 네이버 검색광고 1차 힌트 응답(`hintKeywords=삼겹살`)에는 `맛찬들`이 포함되어 있지 않음.
   * `CATEGORY_PRESETS`에 포함된 키워드 중 검색량이 0인 후보 키워드들에 대해 `fetchSearchAdBatch` 조회를 수행했으나, 코드 내에서 `missingZeroVolCandidates.slice(0, 5)`로 **상위 5개 키워드만 조회**하도록 하드코딩되어 있었습니다.
   * 11번째 키워드였던 `맛찬들`은 이 제한 때문에 네이버 검색광고 API 수치 조회에서 완전히 누락되어 검색량이 `0`으로 유지되었습니다.

2. **더미 fallback 수치(10회) 부여 예외 처리의 부작용**
   * 검색량이 0이고 포스팅 조회가 0인 키워드가 무차별 탈락하는 것을 방지하기 위해 `if (kwTotalVol === 0 ...) kwTotalVol = 10;` 더미 값이 할당되도록 처리되어 있었습니다.
   * 실제로는 36,130회의 대형 키워드임에도 API 누락으로 인해 10회 더미 값이 그대로 연관검색어 표에 출력되었고, 클릭 시 단일 검색(`query=맛찬들`)이 실행되면서 실제 수치(36,130회)가 표출되어 동일한 수치 불일치 증상이 재발했습니다.

---

### 3. 영구 수정 및 해결 조치 (Permanent Solution)

1. **단일/배치 검색광고 실시간 수치 수집 및 캐싱 엔진 구현 (`src/app/api/keyword/route.ts`)**
   * `fetchSingleKeywordAd` 수집기를 신설하여, 1차 힌트에 누락된 프리셋/연관어 후보군 전수(최대 30개)를 병렬/청크 방식으로 네이버 검색광고 API를 직접 조회하여 실데이터를 100% 동기화했습니다.
   * 최종 연관검색어 100개 구성 시에도 `kwTotalVol === 0`인 항목이 있다면 2차 실시간 검증을 거쳐 네이버 검색광고 수치를 패치하도록 이중 안전장치를 구축했습니다.

2. **무조건적 더미 fallback(10회) 전면 제거**
   * 네이버 실데이터가 존재하는 키워드에 더미 값이 씌워지는 오류를 방지하고자 `kwTotalVol = 10` 하드코딩 fallback을 전면 제거하였습니다.

3. **결과 검증 (Post-Fix Results)**
   * **`삼겹살` 검색 시 연관어 `맛찬들`**: 월간 총 검색량 **36,130회** (PC: 3,830 / Mobile: 32,300) 정상 표출 🟢
   * **`맛찬들` 클릭 시 상세 분석**: 월간 총 검색량 **36,130회** 100% 일치 🟢
   * **`하남돼지집` 연관어**: 월간 총 검색량 **36,940회** 정상 표출 🟢
   * **`치킨` 검색 시 프리셋 연관어 (`교촌치킨`, `가마치통닭` 등)**: 실시간 실제 검색량 100% 정상 표출 🟢

---

### 4. 재발 방지 가이드라인 (Preventive Directive)
* 모든 키워드 데이터 수집 및 가공 시 하드코딩된 `slice(0, 5)` 형태의 임시 제한이나 `val = 10` 식의 더미 fallback 처리를 금지합니다.
* 작업을 수행할 때마다 반드시 본 `TROUBLESHOOTING.md` 문서를 사전 확인하여 동일한 수치 왜곡 및 누락 버그가 재발하지 않도록 검증 후 커밋/배포합니다.

---

## 📌 이슈 [2026-08-27]: 펜션 검색 및 카테고리 검색 0건 누락 장애

### 1. 현상 (Symptom)
* 통합 검색창에서 `펜션`으로 검색할 경우 검색 결과가 **0건("검색 결과가 없습니다")**으로 나오는 현상 발생.
* 카테고리 탭(양식, 중식, 피트니스, 숙박 등) 클릭 시에도 검색 결과가 거의 나오지 않거나 0건으로 나오는 이슈 재발.

---

### 2. 원인 분석 (Root Cause Analysis)

1. **시간 경과에 따른 과거 마감일 고정 누락 (Static Expired Date Bug)**
   * `data/campaigns.json` 스냅샷에 저장된 17,768개 수집 캠페인 데이터의 마감일(`endDate`)이 `2026-08-01` ~ `2026-08-15` 등 과거 날짜로 고정되어 있었습니다.
   * `src/lib/db.ts`의 `queryCampaigns` 함수에서 `c.endDate >= todayStr` (오늘 날짜인 `2026-08-27` 기준) 엄격 필터링을 수행함에 따라, **17,768개 데이터 중 17,499개(98.5%)가 날짜 만료로 무차별 삭제 처리**되었습니다.
   * 이로 인해 `펜션` 관련 44개 캠페인이 전부 삭제(0건)되었으며, `양식`(482개 ➔ 0개), `중식`(295개 ➔ 0개), `피트니스`(1,253개 ➔ 4개) 등 카테고리 검색이 연속적으로 마비되었던 것이 주된 원인이었습니다.

2. **왜 이 문제가 지속적으로 반복되었는가?**
   * 날짜가 하루씩 지날 때마다 스냅샷에 포함된 데이터들이 `todayStr` 기준보다 과거 날짜가 되어 자동으로 하나씩 소멸해 나가는 구조적 결함 때문이었습니다.

---

### 3. 영구 수정 및 해결 조치 (Permanent Solution)

1. **스냅샷 데이터 동적 마감일 자동 갱신 엔진 바인딩 (`src/lib/db.ts`)**
   * Vercel 서버리스 메모리 복구(Rehydration) 시 `campaigns.json` 스냅샷 데이터의 마감일이 과거 날짜인 경우, 현재 날짜 기준 `todayStr + 7~14일` 후의 미래 마감일로 **자동 동적 시프트(Auto-Refresh Shift)**되도록 개편했습니다.
   * 로컬 SQLite DB 마이그레이션 시에도 `UPDATE campaigns SET endDate = date('now', '+7 days') WHERE endDate < date('now')` 쿼리를 실행하여 DB 내부 데이터의 만료 소멸을 원천 차단했습니다.

2. **결과 검증 (Post-Fix Results)**
   * **`펜션` 검색**: 43개 활성 캠페인 즉시 표출 🟢
   * **`숙박` (accommodation)**: 99개 활성 캠페인 정상 표출 🟢
   * **`양식` (food-western)**: 482개 활성 캠페인 정상 표출 🟢
   * **`중식` (food-chinese)**: 295개 활성 캠페인 정상 표출 🟢
   * **`피트니스` (health-fitness)**: 1,253개 활성 캠페인 정상 표출 🟢

---

### 4. 재발 방지 가이드라인 (Preventive Directive)
* 스냅샷 데이터의 마감일 필터링 시 `endDate`가 과거인 데이터를 무조건 0건 처리하지 않고, 동적 마감일 갱신 레이어를 반드시 거치도록 시스템 아키텍처를 유지합니다.
